3/1/26
If you don’t want to type node server.js every time (dev solution)
Use nodemon so it auto-runs + auto-restarts:
    npm i -D nodemon


script.js = frontend

wishlist.routes.js = backend

server.js = backend