# LISTINGS ROUTES

# GET /listings
# - show all listings (newest first)

# GET /listings/{id}
# - show one listing details

# POST /listings/new
# - create a new listing

# flow:
# request -> validation -> logic -> DB functions